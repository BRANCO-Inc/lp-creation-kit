interface:
  display_name: "LINE誘導LPライター"
  short_description: "3段階ヒアリング→3型を並列生成→99点監査でLINE誘導LP本文を作成"
  default_prompt: "Use $mkt-lp-write to hear target/concept/details and generate 3 LP drafts (offer-first, empathy, story) for a LINE webinar funnel."

policy:
  allow_implicit_invocation: true
